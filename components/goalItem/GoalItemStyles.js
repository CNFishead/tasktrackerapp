import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  goalItem: {
    margin: 8,
    borderRadius: 20,
    backgroundColor: "#5e0acc",
  },
  goalText: {
    padding: 8,
    color: "white",
  },
});